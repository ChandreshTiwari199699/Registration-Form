# Registration-Form
A user registration system using JavaScript and MongoDB. Includes a frontend form built with HTML/CSS/JS and a backend with Node.js/Express for handling submissions. MongoDB stores user data. Clone, set up environment variables, and start the server to use
